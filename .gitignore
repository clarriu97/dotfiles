# Local-only documentation (kept on disk, not versioned)
docs/

# Backups created by the installer's link_file helper
*.bak-*

# Git bundles (transfer artifacts, not versioned)
*.bundle

# macOS metadata
.DS_Store
