# Base packages (Terminal option) for Ubuntu/Debian, installable from the
# default repos. One package per line; '#' is a comment.
# Packages needing external repos (VS Code, Brave, Warp) are handled in lib/ubuntu.sh.
zsh
git
wget
curl
gpg
unzip
fontconfig
tox
xclip
lsd
fzf
tldr
bat
neofetch
